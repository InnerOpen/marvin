{{- if .Values.route.enabled }}
{{- if .Capabilities.APIVersions.Has "route.openshift.io/v1" }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "marvin.fullname" . }}
  labels:
    {{- include "marvin.labels" . | nindent 4 }}
  {{- with .Values.route.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.route.host }}
  host: {{ .Values.route.host }}
  {{- end }}
  path: {{ .Values.route.path }}
  to:
    kind: Service
    # Split mode serves the UI from its own Service; combined mode has one Service with both ports.
    name: {{ if eq .Values.mode "split" }}{{ include "marvin.fullname" . }}-frontend{{ else }}{{ include "marvin.fullname" . }}{{ end }}
    weight: 100
  port:
    targetPort: {{ if eq .Values.mode "split" }}frontend{{ else }}{{ .Values.route.targetPort | default "http" }}{{ end }}
  {{- if .Values.route.tls.enabled }}
  tls:
    termination: {{ .Values.route.tls.termination }}
    {{- if .Values.route.tls.insecureEdgeTerminationPolicy }}
    insecureEdgeTerminationPolicy: {{ .Values.route.tls.insecureEdgeTerminationPolicy }}
    {{- end }}
  {{- end }}
  wildcardPolicy: None
{{- end }}
{{- end }}
